cd "C:\N Projects\docmind\backend"
venv\Scripts\activate.bat
pip freeze > requirements.txt
```

**Create `Procfile`** (no extension) in `backend` folder:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT